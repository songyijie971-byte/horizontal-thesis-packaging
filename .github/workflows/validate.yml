name: Validate Skill

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate skill
        shell: pwsh
        run: ./scripts/validate-skill.ps1
